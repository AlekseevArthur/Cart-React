import {Products} from '../utilities'

test('we can import ititial product list ', () => {
    expect(Products).toBeTruthy();
});

test('Initital product list has all data that we need ', () => {
    expect(Products).toBeTruthy();
});